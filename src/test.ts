let g = 'apondelio'


if (g.includes('ond')) {


    console.log("ano")

   // console.log('Delka promene a je ' + a.length + ' znaku.')

    console.log(`Delka promene a je ${g.length} znaku.`)

    // It is! But why??
    
}
    else (console.log ("ne"));


    /*if (1 < x < 3) {
    // True for *any* value of x!
    } */

    let hula:string = "obsahuje"

    if (hula.includes("obsa") ) {
        console.log("ano")
        console.log(hula.length)
        console.log(`Toto ${hula.length} je výsledek`)
    }